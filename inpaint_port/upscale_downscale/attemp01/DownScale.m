function result = DownScale(data)
    [imageHeight,imageWidth] = size(data);
    height = floor(imageHeight/2);
    width = floor(imageWidth/2);
    result = zeros(height,width);
    for i = 1:height
        for j = 1:width
            
        end
    end
end